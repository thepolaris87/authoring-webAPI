export { default } from './core';

// // create a new XML document
// const xmlDoc = document.implementation.createDocument(null, 'root');

// // // create a new child element
// const childElem = xmlDoc.createElement('child');
// childElem.setAttribute('name', 'example');
// childElem.textContent = 'This is an example child element.';

// // add the child element to the root element
// xmlDoc.documentElement.appendChild(childElem);

// // serialize the XML document to a string
// const xmlString = new XMLSerializer().serializeToString(xmlDoc);
// console.log(xmlString)
